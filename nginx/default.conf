server {
    listen 80;
    server_name localhost smartshop.net www.smartshop.net 157.90.149.223;

    root /usr/share/nginx/html;
    index index.html;

    # Backend API Proxy
    location /api/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Django Admin Proxy
    location /admin/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Static Files (Served by Nginx via shared volume)
    location /static/ {
        alias /backend_static/;
    }

    # Media Files (Served by Nginx via shared volume)
    location /media/ {
        alias /backend_media/;
    }

    # Frontend (SPA Fallback)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
